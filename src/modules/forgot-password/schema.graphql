# import Error from "../../shared.graphql"

type Query {
  forgotPasswordQuery: String
}

type Mutation {
  sendForgotPasswordEmail(email: String!): [Error!]
  forgotPasswordReset(newPassword: String!, key: String!): [Error!]
}
